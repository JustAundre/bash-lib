#!/usr/bin/env bash
confirm() {
	while [[ ! "${answer}" =~ ^[YyNn]$ ]]; do
		clear -x
		[[ -n "${PS3}" ]] && printf -- '%s\n' "${PS3}"
		printf "%s? [y/n]: \n" "${1}"
		read -re answer
		answer="${answer:0:1}"
	done
	[[ "${answer}" =~ ^[Yy]$ ]]
}
export -f confirm
